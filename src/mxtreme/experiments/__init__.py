"""Whole experiments, each a subpackage: parameters, protocol, execution on the rig, and the
report afterwards. Imports nothing; each experiment is imported by name::

    from mxtreme.experiments import associative
"""
